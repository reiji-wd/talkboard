<h2 class="text-center">最新のスレッド</h2>
<% @rooms.each do |room| %>
  <div class="p-3 border rounded"><h4 class="mb-0"><%= link_to room.title, room_path(room), class: "text-dark" %></h4></div>
<% end %>
